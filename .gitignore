# Node
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / editors
.DS_Store
Thumbs.db
*.db
.idea/
.vscode/
.claude/

# Environment (root & subpackages)
.env
.env.*
!.env.example
!.env.sample

# Image uploads
uploads/

# SQLite & local data — keep folder, ignore its contents
/data/*
!/data/.gitkeep
# If you store seeds or docs in data/, keep them too:
!/data/README.md
!/data/*.sql

# Defensive ignores (if any DBs escape the folder)
/*.sqlite
server/data/*.db
/*.db-shm
/*.db-wal
**/*.sqlite
**/*.db-shm
**/*.db-wal

# Build artifacts (generic)
dist/
build/
tmp/
temp/
.cache/

# Test artifacts
coverage/
test-results/
playwright-report/
playwright/.cache/
.tmpdb-*/

# Next.js (if built from root)
.next/
out/

# Logs
*.log

# E2E workspace (if present)
e2e-tests/node_modules/
e2e-tests/test-results/
e2e-tests/playwright-report/
web/firebase-admin.json

# DB dumps
*.sql
!docker/mysql/init/*.sql
!e2e-tests/sql/*.sql
!mysql_schema.sql
!server/migrations/*.sql
!scripts/seed-chris-matches.sql
!scripts/seed-chris-bathroom-project.sql

# Local migration scripts
scripts/sqlite_to_mysql_schema.js

# Simulation state
.sim-state.json

# Simulation photo fixtures (binary files — keep locally only)
scripts/sim/fixtures/photos/
scripts/sim/fixtures/builder-photos/

.firebase-emulator-data/

# Pitch deck and internal planning artefacts — local-only.
# Generated from scripts/build-pitch-deck.py, build-lighthouse-deck.py,
# and build-vault-deck.py. Contains investor-facing copy, financial
# figures, draft founder bio, and binary .pptx files. Regenerate from
# the scripts when needed; never commit the outputs.
pitch-deck/
project-lighthouse/
project-vault/
.playwright-mcp/
.superpowers/
docs/
e2e-tests/test-results-pw-ui-1/

# Local-only design exploration mocks - never committed.
# Anything under web/pages/mocks/ is throwaway page-level mock work.
web/pages/mocks/
web/pages/admin/tradesmen-leaderboard.old.tsx.bak
