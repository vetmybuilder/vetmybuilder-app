// import { type APIRequestContext } from "@playwright/test";
// import { ProjectsApi } from "../api-utils/projects-api";

// /** Build clients on a fresh authed context and return both. */
// export async function apiContextFactory(
//   authedRequest: () => Promise<APIRequestContext>
// ) {
//   const ctx = await authedRequest();
//   const clients = {
//     projects: new ProjectsApi(ctx),
//     // add more: users, recommendations, etc.
//   };
//   async function dispose() {
//     await ctx.dispose();
//   }
//   return { clients, dispose };
// }

// /** One-shot helper that auto-disposes the context for you. */
// export async function withApis<T>(
//   authedRequest: () => Promise<APIRequestContext>,
//   fn: (clients: { projects: ProjectsApi }) => Promise<T>
// ): Promise<T> {
//   const ctx = await authedRequest();
//   const clients = { projects: new ProjectsApi(ctx) };
//   try {
//     return await fn(clients);
//   } finally {
//     await ctx.dispose();
//   }
// }
